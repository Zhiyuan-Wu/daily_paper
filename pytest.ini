[pytest]
markers =
    integration: marks tests as integration tests (require network access)
    slow: marks tests as slow (may take more than a few seconds)
    live: marks tests that use real external services/data
    live_research: marks tests that require Claude CLI and may be expensive
